enum BottomTabs { home, trackOrder, sellCar, saved }

enum DeliveryAddress { one, two, three }

enum CarCondition { newCar, used }

enum SellType { fixed, bidding }

enum DealerType { kenya, uk }

enum SellingReason { import, local }

enum MembershipLevel { platinum, gold, silver, bronze }
